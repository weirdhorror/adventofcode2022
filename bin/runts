#!/usr/bin/env bash
# Compile a single TypeScript file and run via node

if [[ ! -f "./package.json" ]]; then
  echo "error: run tscr from inside typescript folder"
  exit 1
fi

inputFile="$1.ts"
outputFile="$1.js"

tsc $inputFile
node $outputFile
